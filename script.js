console.log("there was html big bone.");

const student = {
  name: "Mg Mg",
  age: 20,
  ownedVehicle: "bicycle",
};

console.log(`${student.name} have a ${student.ownedVehicle}`);

console.log(`My name is ${student.name}`);
